DB_HOST=db
DB_PORT=5432
DB_NAME=vinbot
DB_USER=postgres
DB_PASSWORD=changeme

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Clé de chiffrement au repos des sessions Vinted (changer en prod)
SESSION_ENCRYPTION_KEY=change-moi

# Chrome DevTools Protocol URL du sidecar connect-browser
CDP_URL=ws://connect-browser:3222

# URL WebSocket noVNC pour le streaming du navigateur (baked in at build time)
NEXT_PUBLIC_NOVNC_URL=ws://localhost:6080
